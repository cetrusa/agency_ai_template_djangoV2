# ==============================================================================
# CONFIGURACIÓN DE DESARROLLO LOCAL
# ==============================================================================
# Copia este archivo a .env y ajusta los valores según tu entorno
#
# ARQUITECTURA DUAL:
# - Docker: usa docker-compose.yml (no necesitas .env)
# - Local: crea .env basado en este ejemplo
# ==============================================================================

# Django Core
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=django-insecure-local-dev-key-change-me-in-production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# ==============================================================================
# OPCIÓN A: MySQL (Laragon o MySQL local)
# ==============================================================================
DJANGO_DB_ENGINE=django.db.backends.mysql
DJANGO_DB_NAME=agency_dashboard
DJANGO_DB_USER=root
DJANGO_DB_PASSWORD=                    # Tu contraseña de MySQL
DJANGO_DB_HOST=127.0.0.1
DJANGO_DB_PORT=3306

# ==============================================================================
# OPCIÓN B: PostgreSQL local (comenta MySQL arriba si usas esto)
# ==============================================================================
# DJANGO_DB_ENGINE=django.db.backends.postgresql
# POSTGRES_DB=agency_dashboard
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# DJANGO_DB_HOST=localhost
# DJANGO_DB_PORT=5432

# ==============================================================================
# OPCIÓN C: SQLite (más simple, sin servidor de BD)
# ==============================================================================
# DJANGO_DB_ENGINE=django.db.backends.sqlite3
# DJANGO_DB_NAME=db.sqlite3
# (deja vacíos USER, PASSWORD, HOST y PORT para SQLite)

# Timezone
DJANGO_TIME_ZONE=America/Mexico_City

# CSRF (opcional para desarrollo local)
DJANGO_CSRF_TRUSTED_ORIGINS=

# Redis (solo si usas cache o background workers)
# REDIS_URL=redis://127.0.0.1:6379/0
